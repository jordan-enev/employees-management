// Basically, this config value should be environment specific and git ignored,
// but according to the demo/test task's requirements, it's perfectly fine to declare it here.
export const API_URL = 'https://9sf7x3qadg.execute-api.eu-west-1.amazonaws.com/api'